[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "Docker_Containers"
dynamic = ["version"]
description = ""
readme = "README.md"
requires-python = ">=3.7"
license = {text = "Apache-2.0"}
authors = [{name = "Benedict Thekkel", email = "bthekkel1@gmail.com"}]
keywords = ['nbdev', 'jupyter', 'notebook', 'python']
classifiers = ["Natural Language :: English", "Intended Audience :: Developers", "Development Status :: 3 - Alpha", "Programming Language :: Python :: 3", "Programming Language :: Python :: 3 :: Only"]
dependencies = []

[project.urls]
Repository = "https://github.com/bthek1/Docker_Containers"
Documentation = "https://bthek1.github.io/Docker_Containers"

[project.entry-points.nbdev]
Docker_Containers = "Docker_Containers._modidx:d"

[tool.setuptools.dynamic]
version = {attr = "Docker_Containers.__version__"}

[tool.setuptools.packages.find]
include = ["Docker_Containers"]

[tool.nbdev]
